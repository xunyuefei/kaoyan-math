# 考研数学 SOP 题解知识库 · AI 助手前置流程与入库规范

> **💡 核心使命**：本仓库是考研数学（高等数学、线性代数）高频题型 SOP 决策解构笔记站。
> 无论在任何新的对话窗口，只要用户发送题目或说“**帮我把这题入库**”、“**录入题目**”，AI 助手必须无条件执行以下全自动处理流程，无需用户手动编辑任何文件。

---

## 🛠️ 触发与全自动执行流程 (Standard Workflow)

当用户在对话框发送题目、或者提出入库需求时：

### 第一步：题目清洗与 SOP 结构化重构
将用户提供的题目内容，严格标准化为以下【6层 SOP 学术典藏结构】：

```markdown
<a id="problem-[题号]"></a>

### 📌 题目 [题号]：[简明题目标题，公式用行内 $ 且不过度嵌套]
题型标签：`[核心考点1]` / `[考点2]` / `[秒杀技巧]`

**原题呈现**：
[完整题目原题，微元规范加间距如 \, \mathrm{d}x，规范积分上下限与集合符号]

> 💡 **核心突破手眼法**：
> IF**: [题干特征识别]
> THEN**: [直击秒杀切入点]

#### 第一步：Layer 1 代数表征（识别考卷符号）
[符号结构、代数形式拆解]

#### 第二步：Layer 2 语义逻辑（直击几何/物理本质或代数同构）
[本质特征、几何构型、对称性或矩阵秩的本质]

#### 第三步：Layer 3 教科书目标（精准对接讲义考点）
[讲义考点对接]

#### 第四步：Layer 4 认知路径（【等价性是如何建立的？】法理本质与最优路径推导）
[最核心的思维逻辑、推导路径]

#### 第五步：Layer 5 演算落地（最优化计算工程）
[最简算力落地过程]

#### 第六步：Layer 6 题型升维（【一题多变与陷阱防线】）
[易错防线与延伸变式]

🎯 **最终正确答案**：
$$[最终标准答案]$$

[🔝 返回目录](#目录)
---
```

### 第二步：智能分流与写入文件 (File Routing)
* **高等数学**（微积分、极限、导数、定积分、二重积分、微分方程、级数、多元微积分等）：
  👉 追加写入至：`content/高等数学_题解集.md` 底部
* **线性代数**（行列式、矩阵、向量组、线性方程组、特征值与特征向量、二次型等）：
  👉 追加写入至：`content/线性代数_题解集.md` 底部

> ⚠️ **注意**：
> 1. 题号需检查对应文件中已有的最大题号，避免冲突重复；
> 2. 真实数据源文件必须在 `content/` 目录下，不要修改根目录下的同名文件。

### 第三步：自动同步与云端发布 (Auto Deploy)
写入完成后，AI 助手**自动在终端执行**以下命令：
```powershell
npm run deploy
```
该命令会自动：
1. 更新 `manifest.json`（按当天日期归档）；
2. 校验排版规范；
3. 执行 `git add`、`git commit` 并 `git push origin main`。

### 第四步：反馈用户
向用户汇报完成，并附带生成的锚点链接：
`https://xunyuefei.github.io/kaoyan-math/#problem-[题号]`

---

## 📐 LaTeX 书写严格规范
* **微元符号**：必须加窄空格与正体，如 `\, \mathrm{d}x`、`\, \mathrm{d}y`、`\, \mathrm{d}r`。
* **分式与根号**：避免复杂公式直接挤在行内，长公式务必使用独立公式块 `$$ ... $$`。
* **矩阵与行列式**：规范使用 `\begin{pmatrix} ... \end{pmatrix}` 或 `\begin{vmatrix} ... \end{vmatrix}`。
* **占位符保护**：严禁在 Markdown 中手写 `<span class="math-ph">` 标签，数学公式一律使用 `$ ... $` 或 `$$ ... $$`。
