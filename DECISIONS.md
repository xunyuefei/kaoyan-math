# 🏛️ 考研数学知识库 · 架构决策记录 (DECISIONS.md)

本文档采用 ADR (Architecture Decision Record) 格式，记录考研数学知识库演进过程中的关键技术与认知决策。

---

## ADR-001: 采用 6 层 SOP 学术典藏结构而非传统题解格式

* **状态**：已采纳 (Accepted)
* **日期**：2026-09-10
* **背景 (Context)**：
  传统的考研数学题解通常只包含“题目”、“解答过程”与“答案”。这种格式导致考生只能机械记忆死步骤，一旦考试出现变式或符号伪装便极易卡壳；此外，散碎的题解无法提炼出具有迁移价值的“手眼法”与“等价性思维”。
* **决策 (Decision)**：
  对所有入库真题强制实施 6 层 SOP 学术典藏重构：
  1. Layer 1 代数表征（识别考卷符号）
  2. Layer 2 语义逻辑（直击几何/物理本质或代数同构）
  3. Layer 3 教科书目标（精准对接讲义考点）
  4. Layer 4 认知路径（【等价性是如何建立的？】法理本质与最优路径推导）
  5. Layer 5 演算落地（最优化计算工程）
  6. Layer 6 题型升维（【一题多变与陷阱防线】）
  并在题头前置“💡 核心突破手眼法（IF 特征 -> THEN 秒杀切入点）”。
* **影响与后果 (Consequences)**：
  - 优点：解题思路清晰透明，直击命题法理，极大提升考场瞬时直觉迁移能力。
  - 约束：AI 录入时必须严格按照此模板生成，严禁偷工减料或合并层级。

---

## ADR-002: 数据源与发布端物理隔离及绝对路径防线

* **状态**：已采纳 (Accepted)
* **日期**：2026-09-12
* **背景 (Context)**：
  历史构建中根目录下曾存在 `高等数学_题解集.md` 与 `content/高等数学_题解集.md` 两个同名文件。早期 AI 助手曾错误地将新题目写入根目录文件，导致前端构建流水线未检测到更新，真题数据丢失。
* **决策 (Decision)**：
  1. 明确 `content/` 为唯一真题数据源目录；
  2. 根目录同名文件仅作为历史产物封存；
  3. 在 `AGENTS.md` 和自动化校验脚本中设置绝对路径防线，任何写入操作必须定位在 `content/*.md`。
* **影响与后果 (Consequences)**：
  彻底解决了数据写入冲突与版本错乱问题。

---

## ADR-003: 前端单页轻量化同构与客户端离线状态存储

* **状态**：已采纳 (Accepted)
* **日期**：2026-09-15
* **背景 (Context)**：
  用户需要在 PC 浏览器、平板以及手机 Wi-Fi 局域网下随时随地刷题，并希望能够对题目进行星标（重要/常错/待重刷）以及记录个性化备忘心得。若引入重量级后端数据库，会极大增加部署复杂性与脆弱性。
* **决策 (Decision)**：
  1. 题解数据采用静态 Markdown + 预构建 `manifest.json` 索引；
  2. 前端采用原生 Vanilla JavaScript (SPA)，利用 MathJax/KaTeX 客户端极速排版；
  3. 星标状态与随手记备忘统一持久化在浏览器的 `localStorage` 中；
  4. 支持星标聚合看板与随手记目录快速导览。
* **影响与后果 (Consequences)**：
  零运维成本、无需服务器后端、秒级响应、支持完全离线与静态 GitHub Pages 部署。

---

## ADR-004: 原始题号绝对优先原则与独立分流防线

* **状态**：已采纳 (Accepted)
* **日期**：2026-09-16
* **背景 (Context)**：
  用户在做讲义或习题集时，习惯指明如“第 38 题”、“第 39 题”。若 AI 自作聪明重新编号或将多道题合并为一张卡片，将破坏用户真题与做题纸的对应关系。
* **决策 (Decision)**：
  1. 判定优先级：用户给出的原始题号 > 递增自动计算；
  2. 多题输入时强制拆分为独立题解卡片，每张卡片独占 `#problem-[ID]` 锚点。
* **影响与后果 (Consequences)**：
  保证了题号真实性和卡片定位的精准性。

---

## ADR-005: 禁用 AI 内部无头浏览器沙盒访问本地端口，坚守系统原生浏览器与自动化测试基准

* **状态**：已采纳 (Accepted)
* **日期**：2026-09-19
* **背景 (Context)**：
  2026-09-18 深夜迭代中，AI 助手在完成代码修改后，擅自启动内部 `browser_subagent` 尝试对 `http://localhost:5210/` 截图验证。由于 IDE 内置无头浏览器沙盒在与本地 Windows loopback 端口握手时超时（`open_browser_url: action timed out`），导致子智能体进入死循环重试并锁死等待，阻断了正常用户交互流程。
* **决策 (Decision)**：
  1. **严禁擅自启动 browser_subagent**：常规功能开发与题库录入流程中，禁止 AI 自发调用无头浏览器沙盒对 localhost 进行视觉截屏；仅在用户明确发出“用浏览器打开查看截图”指令时方可尝试，且一旦发生一次超时立即终止，绝不循环重试。
  2. **依托双重高可靠验证防线**：
     - 代码与数据合规性完全交由 `python tests/test_math_integrity.py` 与 `scripts/sync.js` 自动化保障（秒级且 100% 确定性）；
     - 用户侧视觉预览统一由 `deploy.js` 或 `【打开数学题解网站】.bat` 唤起用户的真实系统默认浏览器（Chrome / Edge），零沙盒网络兼容隐患。
* **影响与后果 (Consequences)**：
  彻底根除因无头浏览器沙盒网络超时导致的假死与等待问题，交付流程更轻量、敏捷、可预期。

