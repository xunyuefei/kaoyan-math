# 🤝 考研数学知识库 · AI 助手交接手册 (HANDOFF.md)

> **致接手本项目的 AI 助手或维护者**：
> 本知识库承载着用户考研数学复习的全部高频题目和解题认知体系。为确保您的介入如丝般顺滑，请务必仔细阅读本交接手册。

---

## 📌 当前项目基准状态 (Current State)

* **工程版本**：v2.4.0 (支持全景星标体系 + 随手记目录 + 移动端自适应)
* **题目存量**：
  * 高等数学：250+ 道 SOP 标准卡片 (`content/高等数学_题解集.md`)
  * 线性代数：270+ 道 SOP 标准卡片 (`content/线性代数_题解集.md`)
  * 全量题解总数已达 524+ 道，每道题均拥有唯一的 `#problem-[ID]` 锚点。
* **部署状态**：
  * 本地服务：`http://localhost:5210/` (静态服务)
  * 云端线上：[https://xunyuefei.github.io/kaoyan-math/](https://xunyuefei.github.io/kaoyan-math/)
  * Git 分支：`main`，与 `origin/main` 保持强同步。

---

## 🛡️ 核心防线与不可触碰之红线 (Critical Invariants)

接手本项目的任何 AI 助手在执行写操作时，**必须严格恪守以下 5 条铁律**：

1. **绝对路径与真实数据源**：
   * 写入题目必须追加至 `content/高等数学_题解集.md` 或 `content/线性代数_题解集.md`；
   * ❌ **绝不可**写入根目录下的同名文件！根目录文件仅为历史构建产物。
2. **原始题号绝对优先**：
   * 若用户发送的题目中自带题号（例如 `38.`、`39.`、`题 524`），**必须使用用户的原始题号**；
   * 仅在用户完全未提供题号时，才自动取当前最大题号 +1。
3. **多题严格独立拆分**：
   * 用户若一次性输入多道题目，**必须拆分为多个完全独立的卡片块**；
   * 严禁合并为一题，每道题必须独占专属锚点、元数据行与 6 层 SOP。
4. **Layer 标题独占换行**：
   * `#### 第X步：Layer X ...` 标题行后**必须换行**后再写正文，严禁标题与正文连在同一行，否则前端解析器会渲染错位。
5. **LaTeX 严谨规范**：
   * 微元必须带窄空格和正体：`\, \mathrm{d}x`、`\, \mathrm{d}y`、`\, \mathrm{d}t`；
   * 行内公式使用 `$ ... $`，复杂公式使用 `$$ ... $$`；
   * 严禁手写 `<span class="math-ph">` 等前端内部占位标签。

---

## 🧭 用户交互与日常指令协议

* **用户输入协议**：
  用户在对话中发送新题目时，第一行固定输入：
  `[来源题集] [标准章节]`，例如：`严选题 二重积分` 或 `660题 特征值与特征向量`。
* **分流规则**：
  * 高数章节：`函数与极限`、`一元函数微分`、`一元函数积分`、`常微分方程`、`多元函数微分`、`二重积分`。
  * 线代章节：`行列式`、`矩阵`、`n维向量`、`线性方程组`、`特征值与特征向量`、`二次型`。
* **闭环交付命令**：
  入库完成后，必须在终端执行：
  ```powershell
  npm run deploy
  ```
  该命令会自动更新 `manifest.json`，执行 git commit 并 push 到 GitHub。

---

## 🔍 关键文件导航

| 文件路径 | 作用与修改建议 |
| :--- | :--- |
| `AGENTS.md` | AI 行为准则，每次对话必读 |
| `content/*.md` | 真题卡片的主数据源，题目追加于此 |
| `manifest.json` | 前端渲染的大纲目录索引，由脚本自动生成，通常不手写 |
| `scripts/sync_manifest.js` | 题解目录提取同步器，由 `npm run deploy` 自动调用 |
| `tests/test_math_integrity.py` | 题库完整性测试，推送前必跑 |
| `index.html` / `app.js` | 前端单页应用，负责题解卡片流式渲染与交互 |

---

## 📋 接手检查清单 (Checklist for Next Agent)

在开始任何新任务前，请执行以下检查：
- [ ] 检查 `git status` 确保工作树干净；
- [ ] 运行 `python tests/test_math_integrity.py` 确保全库题解合规；
- [ ] 阅读用户最新的指示，明确是录入新题、调整功能还是排查排版；
- [ ] 任务完成后运行 `npm run deploy` 自动同步并反馈用户锚点链接。
